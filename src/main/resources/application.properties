spring.application.name=tiendaOnline

spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/tiendaOnline}
spring.datasource.username=${JDBC_DATABASE_USER:postgres}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:02072013}

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=xandersoftworks@gmail.com
spring.mail.password=ruvz ggxm duam pcla
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

file.upload-dir=src/main/resources/static/uploads

jwt.private-key-location=${JWT_PRIVATE_KEY_LOCATION:file:private_key.pem}
jwt.public-key-location=${JWT_PUBLIC_KEY_LOCATION:file:public_key.pem}
jwt.issuer=${JWT_ISSUER:tienda-online-api}
jwt.audience=${JWT_AUDIENCE:tienda-online-client}
jwt.expiration=${JWT_EXPIRATION_MS:900000}

imagen.perfil.por.defecto=/images/sinImagenPerfil.webp

server.port=${PORT:8080}

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:8080}

# Cache - región productos
cache.productos.ttl-minutes=10
cache.productos.max-size=100

# Cache - región busquedaProductos
cache.busqueda-productos.ttl-minutes=5
cache.busqueda-productos.max-size=200

# Cache - región productoPorId
cache.producto-por-id.ttl-minutes=10
cache.producto-por-id.max-size=500

# IdempotencyStore
idempotency.ttl-hours=24
idempotency.max-size=10000
